<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
>
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <php>
        <env name="APP_ENV" value="testing"/>
        <!--
            Fijado el 2026-09-09: sin esto, la suite compara textos en
            inglés (ver p.ej. MachineForceDeleteImpactTest) contra lo que
            sea que traiga el .env de quien la corra. Detectado a mano el
            mismo día: cambiar APP_LOCALE en el .env local a "es" rompió el
            test del modal, porque phpunit.xml no declaraba ningún locale
            propio y la suite heredaba el de afuera en silencio.
        -->
        <env name="APP_LOCALE" value="en"/>
        <env name="APP_FALLBACK_LOCALE" value="en"/>
        <env name="APP_MAINTENANCE_DRIVER" value="file"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="CACHE_STORE" value="array"/>
        <env name="DB_CONNECTION" value="sqlite"/>
        <env name="DB_DATABASE" value=":memory:"/>
        <!--
            Fijado el 2026-09-18 (hallazgo 4): config/accounts.php ya trae
            'password' como default para APP_ENV=testing, así que esta línea
            es cinturón y tirantes — no dependas del default implícito si
            alguien corre la suite con otro APP_ENV.
        -->
        <env name="DEMO_SEED_PASSWORD" value="password"/>
        <env name="MAIL_MAILER" value="array"/>
        <env name="PULSE_ENABLED" value="false"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <env name="SESSION_DRIVER" value="array"/>
        <env name="TELESCOPE_ENABLED" value="false"/>
    </php>
</phpunit>
